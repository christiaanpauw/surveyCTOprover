
# Build the Hedera poster helper
post-hcs-build:
	go build -o bin/post_hcs post_hcs.go

# Run an attested container execution and post its hash to Hedera.
# attested-run.sh reads ATT_FLAGS, ATT_IMAGE and ATT_CMD from the environment
# and falls back to values in a .env file if they are not set.
# Example:
#   ATT_IMAGE="registry/org/app@sha256:ABCD..." make attest-run
attest-run: post-hcs-build
        ./attested-run.sh
