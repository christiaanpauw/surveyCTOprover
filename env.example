# Hedera configuration
HEDERA_NETWORK=testnet
OPERATOR_ID=0.0.xxxxxx
OPERATOR_KEY=302e020100300506032b657004220420...
TOPIC_ID=0.0.yyyyyy

# Server port
PORT=8080

# Docker attested run configuration
# Optional docker run flags, e.g. "-e FOO=bar -v /data:/data"
ATT_FLAGS=
# Image reference with immutable digest
ATT_IMAGE=registry/org/app@sha256:...
# Command and arguments to run inside the container
ATT_CMD=
